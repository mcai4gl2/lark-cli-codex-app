name: CI

on:
  push:
  pull_request:

jobs:
  test:
    name: Go tests
    runs-on: ubuntu-latest
    container:
      image: golang:1.24

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run unit tests
        run: go test ./...
