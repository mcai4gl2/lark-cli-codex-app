# 2026-06-10

## Slack Reaction Commands

### Problem / Goal
- Implement the optional Slack reaction commands from Phase 3 of `slack-migration.md`.
- Keep command output compact and use Docker-based Go formatting, tests, and build verification.

### Problems Encountered
- Docker build requires marking `/work` as a git safe directory inside the disposable container so Go VCS stamping can read repository metadata.

### Key Design Decisions
- Mirrored the existing Lark command shape with `slack msg react`, `slack msg react list`, and `slack msg react remove`.
- Used Slack message coordinates directly: `--channel` plus `--ts`.
- Normalized reaction names by trimming surrounding colons so both `eyes` and `:eyes:` send Slack the expected `name`.

### Solution / Implementation
- Added Slack Web API client support for `reactions.add`, `reactions.get`, and `reactions.remove`.
- Added compact reaction output types for mutation results and reaction listing.
- Added CLI wiring and flags for add, list, and remove reaction commands.
- Updated Phase 3 in `slack-migration.md` to mark reactions complete and document required scopes.

### Results
- Red test run confirmed missing reaction APIs before implementation:
  - `docker run --rm -v "$PWD:/work" -w /work golang:1.24 go test ./internal/slack ./internal/cmd`
- Formatting:
  - `docker run --rm -v "$PWD:/work" -w /work golang:1.24 gofmt -w internal/slack/client.go internal/slack/client_test.go internal/cmd/slack.go internal/cmd/slack_test.go`
- Focused tests:
  - `docker run --rm -v "$PWD:/work" -w /work golang:1.24 go test ./internal/slack ./internal/cmd`
- Full verification:
  - `docker run --rm -v "$PWD:/work" -w /work golang:1.24 go test ./...`
- CLI build:
  - `docker run --rm -v "$PWD:/work" -w /work golang:1.24 sh -c 'git config --global --add safe.directory /work && go build -ldflags "-s -w" -o ./lark ./cmd/lark'`

### Files Modified
- `internal/slack/client.go` - added reaction API methods, output types, and reaction name normalization.
- `internal/slack/client_test.go` - added tests for Slack reaction add, get, and remove payloads.
- `internal/cmd/slack.go` - added Slack reaction add, list, and remove commands.
- `internal/cmd/slack_test.go` - added reaction command registration coverage.
- `slack-migration.md` - marked Phase 3 reaction commands complete and documented scopes.
- `devlogs/2026-06-10.md` - recorded this session.

## Slack User Guide

### Problem / Goal
- Add a user-facing guide for setting up the Slack app, required permissions, recommended gateway operation, and CLI usage.
- Base the setup instructions on the current Slack command/config surface and official Slack documentation.

### Problems Encountered
- None - documentation-only change with no unexpected issues.

### Key Design Decisions
- Added a standalone `USER_GUIDE.md` instead of expanding the Lark-focused README.
- Recommended Socket Mode as the default run mode because it avoids a public callback URL.
- Split scopes into minimum gateway scopes, CLI history/reaction scopes, and optional metadata scopes.

### Solution / Implementation
- Documented Slack app creation, Socket Mode app-level token setup, OAuth bot scopes, event subscriptions, bot channel invitation, local environment variables, gateway operation, message commands, smoke tests, and troubleshooting.
- Included links to official Slack docs for Socket Mode, Events API, tokens, scopes, message APIs, and reaction APIs.

### Results
- Documentation was reviewed locally with `rg` and `sed` for placeholders, command names, and environment variable consistency.
- Go tests were not rerun because this was a Markdown-only change.

### Files Modified
- `USER_GUIDE.md` - added Slack setup and usage guide.
- `devlogs/2026-06-10.md` - appended this documentation entry.

## Slack Workspace And Memory Guide

### Problem / Goal
- Explain how the local Codex workspace folder is chosen when Slack tasks are dispatched.
- Add a recommended setup for two usage modes: generic chat with durable notes and project-specific work.
- Document the proposed future direction for channel/thread folders and conversation summaries.

### Problems Encountered
- None - documentation-only change with no unexpected issues.

### Key Design Decisions
- Recommended separate Slack apps and gateway processes for generic chat and project-specific work to keep identity, logs, tokens, scopes, and workspaces cleanly separated.
- Documented current limitations clearly: event logs exist, but thread history and summaries are not automatically injected into the Codex prompt yet.
- Kept the memory folder design as a future extension rather than implying current support.

### Solution / Implementation
- Added a `How The Local Workspace Folder Works` section to `USER_GUIDE.md`.
- Added a `Recommended Two-Mode Setup` section with generic chat and project-specific gateway examples.
- Added launch-script examples and a proposed `.slack/conversations/...` layout for future memory/audit support.

### Results
- Documentation was reviewed locally with `rg` and `sed` for numbering, placeholder text, command names, and environment variable consistency.
- Go tests were not rerun because this was a Markdown-only change.

### Files Modified
- `USER_GUIDE.md` - documented workspace selection, two-mode setup, and future memory extension.
- `devlogs/2026-06-10.md` - appended this documentation entry.

## Slack Memory Extension Plan

### Problem / Goal
- Create an implementation plan for extending Slack memory support based on the `USER_GUIDE.md` memory/audit design.
- Save the plan at the user-requested path `plans/20260610_memory-extension.md`.

### Problems Encountered
- None - planning-only change with no unexpected issues.

### Key Design Decisions
- Scoped the first implementation to filesystem-backed audit logs, explicit Markdown memory files, and prompt context injection.
- Deferred automatic LLM summarization to future work so the first implementation remains deterministic and testable.
- Planned a focused `internal/slackmemory` package rather than expanding the Slack gateway or agent files with storage details.

### Solution / Implementation
- Added a task-by-task TDD implementation plan covering storage layout, context loading, agent hooks, Slack gateway wiring, config/flags, CLI memory commands, docs, and verification.
- Included concrete file paths, test snippets, implementation snippets, Docker commands, expected red/green outcomes, and commit points.

### Results
- Documentation/plan was reviewed locally with `rg`, `sed`, and `git status`.
- Go tests were not rerun because this was a Markdown-only planning change.

### Files Modified
- `plans/20260610_memory-extension.md` - added the memory extension implementation plan.
- `devlogs/2026-06-10.md` - appended this planning entry.
