# 2026-06-14

## Slack Recover Mode and Processing Reaction

### Problem / Goal
- Avoid requiring users to repost Slack thread messages after the Socket Mode gateway reconnects or restarts.
- Persist known participating Slack threads and last processed timestamps so the gateway can catch up missed replies.
- Add a visible processing reaction while Codex is working on a Slack message, then remove it when processing finishes.

### Problems Encountered
- A final review found that the first catch-up implementation advanced recovery state before message processing succeeded, which could skip messages permanently after event-log, memory, or desktop enqueue failures.
- A second review found another early state advancement path inside `processEntry` before desktop enqueue could fail.
- Both issues were fixed with regression tests so `LastProcessedTS` advances only after successful processing for normalized user messages.

### Key Design Decisions
- Default recover mode is `thread`, which catches up messages in known participating threads without scanning whole channels.
- `mention-dm` and `off` modes are configurable via CLI/config/env.
- Recovery state lives under `.slack/conversations/.state/recover-state.json`.
- The processing reaction defaults to `eyes` and can be disabled with an empty `processing_reaction` or `--processing-reaction ""`.
- Catch-up intentionally marks normalized-out bot/subtype/blank messages processed to avoid repeated scanning.

### Solution / Implementation
- Added Slack recovery state persistence and catch-up normalization for messages returned by `conversations.replies`.
- Added gateway catch-up on startup and after successful Socket Mode reconnects.
- Added agent processing lifecycle hooks and Slack reaction observer for add/remove around Codex processing.
- Added CLI flags and config/env support for recover mode and processing reaction.
- Updated user/deployment docs and saved the implementation plan in `plans/20260613_recover-mode.md`.

### Results
- Verified focused packages with Docker Go 1.24: `go test ./internal/slack ./internal/agent ./internal/cmd ./internal/config`.
- Verified full suite with Docker Go 1.24: `go test ./...`.
- Built the CLI with Docker Go 1.24 and installed it to `/home/ligeng/.local/bin/lark`.
- Restarted the homelab Slack gateway; final live PID was `1112378`.
- Verified startup JSON includes `"recover_mode": "thread"` and `"processing_reaction": "eyes"`.
- Verified the gateway had active Slack HTTPS/WebSocket connections after restart.

### Files Modified
- `internal/slack/recover.go` - added durable recovery state store.
- `internal/slack/events.go` - added catch-up message normalization.
- `internal/slack/gateway.go` - added recover mode, catch-up, processing reaction observer, and safer state advancement.
- `internal/agent/codex.go` - added processing lifecycle observer hooks.
- `internal/cmd/slack.go` - added recover mode and processing reaction flags/startup JSON.
- `internal/config/config.go` - added config defaults/env/getters.
- `USER_GUIDE.md` and `docs/codex-slack-deployment.md` - documented recover mode and processing reaction behavior.
- `plans/20260613_recover-mode.md` - recorded the approved implementation plan.

## Slack Recovery Catch-Up API Fix

### Problem / Goal
- Check the latest homelab Slack gateway deployment logs for issues in the new recovery code.
- Fix any confirmed production issue that prevents restart/reconnect catch-up from working.

### Problems Encountered
- The gateway stayed alive and reconnected successfully, but every recovery catch-up call failed with `slack conversations.replies failed: invalid_arguments`.
- Live Slack API probes showed JSON POST requests to `conversations.replies` were rejected as missing `channel` and `ts`, while the same parameters worked as GET query parameters.
- The initial Docker build failed on VCS stamping because the mounted workspace was not marked as a safe Git directory inside the container; rerunning with `git config --global --add safe.directory /work` fixed the build.

### Key Design Decisions
- Keep JSON POST for existing Slack mutation paths, including `chat.postMessage` and reaction mutations.
- Use GET query parameters for read-only conversation methods, matching the live Slack API behavior for `conversations.history` and `conversations.replies`.

### Solution / Implementation
- Added a GET request helper to the Slack client.
- Switched `History` and `Thread` to send query parameters instead of JSON bodies.
- Updated Slack client and gateway catch-up tests to assert GET/query request shape.
- Rebuilt and installed `/home/ligeng/.local/bin/lark`, then restarted the homelab gateway.

### Results
- Verified focused package with Docker Go 1.24: `go test ./internal/slack`.
- Verified full suite with Docker Go 1.24: `go test ./...`.
- Verified the installed CLI can read the production Slack thread via `lark slack msg thread --channel C0BA8EUDT61 --thread-ts 1781338388.507139 --limit 1`.
- Restarted the homelab Slack gateway; live PID was `1194252`.
- Fresh startup logs no longer showed `Slack recovery catch-up failed ... invalid_arguments`, and recovery state advanced for tracked threads.

### Files Modified
- `internal/slack/client.go` - added GET helper and moved conversation read methods to query parameters.
- `internal/slack/client_test.go` - updated conversation read tests to assert GET/query request shape.
- `internal/slack/gateway_test.go` - updated catch-up test fixture for the new `conversations.replies` request shape.

## Slack Thread Polling and Quiet Ack

### Problem / Goal
- Confirm why unmentioned Slack thread replies were not triggering immediate Codex responses.
- Make the processing reaction the only acknowledgement by default, without sending the `Received. Working on it.` text reply.
- Keep the deployed homelab gateway aligned with the code change.

### Problems Encountered
- Live logs showed only `app_mention` events arriving through Socket Mode; plain thread replies were absent unless catch-up discovered them.
- Root cause: the gateway only ran Slack thread catch-up on startup and reconnect, so a healthy long-lived socket did not continuously monitor known participating threads.
- The first Docker build failed because Go VCS stamping could not read repository status inside the container; rebuilding with `-buildvcs=false` completed successfully.

### Key Design Decisions
- Add a lightweight recovery poller for known participating threads instead of requiring generic channel message event subscriptions.
- Default the poll interval to one minute to keep Slack Web API usage conservative.
- Keep Slack ack text configurable, but make the Slack default blank and skip blank ack sends entirely.
- Reduce catch-up `conversations.replies` limit to the current documented lower cap used by newer non-Marketplace Slack apps.

### Solution / Implementation
- Added a Slack recovery poll loop while the Socket Mode connection remains healthy.
- Serialized catch-up runs with a mutex so startup, reconnect, and polling catch-up cannot overlap.
- Changed agent ack handling to skip blank/whitespace ack text.
- Changed the Slack ack default from `Received. Working on it.` to blank.
- Rebuilt `/home/ligeng/.local/bin/lark` and restarted the homelab Slack gateway.

### Results
- Watched the new ack and polling tests fail before implementation.
- Verified focused tests with Docker Go 1.24: `go test ./internal/agent ./internal/slack ./internal/config`.
- Verified full suite with Docker Go 1.24: `go test ./...`.
- Built the CLI with Docker Go 1.24 using `go build -buildvcs=false -ldflags "-s -w" -o ./lark ./cmd/lark`.
- Installed the CLI to `/home/ligeng/.local/bin/lark` and restarted the gateway; live PID is `1235383`.
- Verified the restarted gateway is connected and processed an unmentioned plain thread reply via catch-up at `2026-06-14T10:29:57+08:00`.

### Files Modified
- `internal/agent/codex.go` - skipped blank acknowledgement replies.
- `internal/agent/codex_test.go` - added regression coverage for blank ack behavior.
- `internal/config/config.go` - made Slack ack text blank by default.
- `internal/config/slack_config_test.go` - asserted the new Slack ack default.
- `internal/slack/gateway.go` - added continuous recovery polling, catch-up serialization, and the lower thread fetch limit.
- `internal/slack/gateway_test.go` - added regression coverage for polling catch-up while connected.

## Slack Thread Close Reaction Tracking

### Problem / Goal
- Confirm whether a live tracked Slack thread root had been marked with a green check reaction.
- Stop polling tracked threads when their root message has `white_check_mark`.
- Stop polling tracked threads after one day of inactivity.
- Emit a clear log line when a checkmark closes tracking.

### Problems Encountered
- The existing `lark slack msg react list` path used JSON POST for `reactions.get`, and Slack returned `no_item_specified`.
- Live probing with GET query parameters confirmed the checked thread root `1781397740.351729` had `white_check_mark` from user `U09DF4NSQH1`.
- Existing gateway catch-up tests needed no-reaction fixtures because catch-up now checks root reactions before fetching replies.

### Key Design Decisions
- Use Slack `white_check_mark` as the close reaction.
- Record the checkmark log timestamp as detection time because `reactions.get` returns reaction users and counts but not the original reaction-add timestamp.
- Apply the one-day TTL to `last_seen_at` before any Slack API calls, avoiding unnecessary reaction/reply polling for stale tracked threads.

### Solution / Implementation
- Changed `reactions.get` to use GET query parameters.
- Added recovery-store removal for individual tracked threads.
- Added checkmark detection before catch-up reply polling.
- Added a 24-hour tracked-thread TTL.
- Added the requested log format: `thread <thread_ts>, user <user_id> ticked at <timestamp>, stop tracking`.
- Rebuilt and installed `/home/ligeng/.local/bin/lark`, then restarted the homelab Slack gateway.

### Results
- Verified focused Slack tests with Docker Go 1.24: `go test ./internal/slack`.
- Verified full suite with Docker Go 1.24: `go test ./...`.
- Restarted the gateway; live PID is `1296549`.
- Confirmed the live gateway logged `thread 1781397740.351729, user U09DF4NSQH1 ticked at 2026-06-14T11:04:55.70313274+08:00, stop tracking`.
- Confirmed recovery state now tracks only the two remaining thread roots and no longer includes `1781397740.351729`.

### Files Modified
- `internal/slack/client.go` - changed `reactions.get` to GET query parameters.
- `internal/slack/client_test.go` - updated reaction-list request-shape coverage.
- `internal/slack/recover.go` - added tracked-thread removal.
- `internal/slack/recover_test.go` - added recovery removal coverage.
- `internal/slack/gateway.go` - added checkmark close detection and 24-hour TTL pruning.
- `internal/slack/gateway_test.go` - added close-reaction and stale-thread coverage.
