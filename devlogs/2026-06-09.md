# 2026-06-09

## Slack Gateway MVP

### Problem / Goal
- Implement Phase 2 from `slack-migration.md`: add a Slack Socket Mode gateway MVP while preserving the existing shared inbound, agent, and desktop paths.
- Follow the repository requirement to use Docker-based Go formatting, tests, and build verification.

### Problems Encountered
- Docker `go build` initially failed with `error obtaining VCS status: exit status 128`; reran the build after marking `/work` as a safe git directory inside the container.

### Key Design Decisions
- Added a small direct Slack Web API client instead of introducing a larger SDK dependency for the MVP.
- Kept Slack code in `internal/slack` and injected the Slack messenger into existing inbound, agent, and desktop queue paths.
- Used `.slack/` defaults for Slack event logs and desktop tasks so existing `.lark/` state is untouched.

### Solution / Implementation
- Added Slack config/env bindings for app token, bot token, bot user ID, gateway, and agent settings.
- Added Slack Web API support for `chat.postMessage`, `auth.test`, and `apps.connections.open`.
- Added Slack event normalization for `app_mention` and `message.im`, including bot/self-message filtering, mention stripping, thread selection, and neutral event persistence.
- Added `lark slack gateway serve` with agent, event-log, auto-reply, workspace, and desktop-worker flags.
- Added focused tests for config, Web API calls, event normalization, desktop queue routing, and command registration.

### Results
- `docker run --rm -v "$PWD:/work" -w /work golang:1.24 gofmt -w ...`
- `docker run --rm -v "$PWD:/work" -w /work golang:1.24 go test ./internal/config ./internal/slack ./internal/cmd`
- `docker run --rm -v "$PWD:/work" -w /work golang:1.24 go test ./internal/config ./internal/slack ./internal/cmd ./internal/inbound ./internal/agent ./internal/desktop ./internal/gateway ./internal/webhook`
- `docker run --rm -v "$PWD:/work" -w /work golang:1.24 go test ./...`
- `docker run --rm -v "$PWD:/work" -w /work golang:1.24 sh -c 'git config --global --add safe.directory /work && go build -ldflags "-s -w" -o ./lark ./cmd/lark'`

### Files Modified
- `config.example.yaml` - added Slack Socket Mode configuration example.
- `internal/config/config.go` - added Slack config defaults, environment bindings, and accessors.
- `internal/config/slack_config_test.go` - added Slack config tests.
- `internal/slack/client.go` - added minimal Slack Web API messenger.
- `internal/slack/client_test.go` - added Slack Web API client tests.
- `internal/slack/events.go` - added Slack event normalization.
- `internal/slack/events_test.go` - added event normalization and filtering tests.
- `internal/slack/gateway.go` - added Slack Socket Mode gateway service.
- `internal/slack/gateway_test.go` - added desktop-routing gateway test.
- `internal/cmd/slack.go` - added `slack gateway serve` command.
- `internal/cmd/slack_test.go` - added command registration test.
- `internal/cmd/root.go` - registered the Slack command namespace.

## Slack Migration Plan Update

### Problem / Goal
- Update `slack-migration.md` so Phase 2 reflects the implemented Slack Gateway MVP work and remaining manual smoke-test gap.

### Problems Encountered
- None - straightforward documentation update.

### Key Design Decisions
- Marked Phase 2 complete for implemented and automated-test-covered work, while keeping live Slack workspace smoke tests called out separately because they require real app credentials.

### Solution / Implementation
- Converted the Phase 2 checklist to checked items.
- Added implementation notes for config, `.slack/` state defaults, `internal/slack`, command wiring, tests, and Docker verification.
- Added a separate manual smoke-test list for credential-backed Slack workspace validation.

### Results
- Documentation-only change; tests were not rerun for this update.

### Files Modified
- `slack-migration.md` - updated Phase 2 status, checklist, implementation notes, and manual smoke-test caveat.

## Slack CLI Message Commands

### Problem / Goal
- Implement Phase 3 message-command work from `slack-migration.md`.
- Add Slack CLI commands for sending messages, reading channel history, and reading thread replies while preserving compact JSON output and Docker-only Go verification.

### Problems Encountered
- The first Docker build failed with `error obtaining VCS status: exit status 128`; reran the build in a disposable container after adding `/work` to git's safe directories inside that container.

### Key Design Decisions
- Implemented the P1 message commands only and deferred optional reaction commands because they require additional Slack reaction scopes and were explicitly optional in the migration plan.
- Kept Slack history/thread support to one compact Web API page with `has_more` surfaced, rather than adding cursor pagination before there is a concrete CLI need.

### Solution / Implementation
- Added Slack Web API client methods for `conversations.history`, `conversations.replies`, and send-result output from `chat.postMessage`.
- Added `lark slack msg send`, `lark slack msg history`, and `lark slack msg thread` command wiring.
- Added focused tests for Slack API payloads and Slack message command registration.
- Updated `slack-migration.md` Phase 3 status and implementation notes.

### Results
- Red test run confirmed missing Phase 3 API before implementation:
  - `docker run --rm -v "$PWD:/work" -w /work golang:1.24 go test ./internal/slack ./internal/cmd`
- Formatting:
  - `docker run --rm -v "$PWD:/work" -w /work golang:1.24 gofmt -w internal/slack/client.go internal/slack/client_test.go internal/cmd/slack.go internal/cmd/slack_test.go`
- Focused tests:
  - `docker run --rm -v "$PWD:/work" -w /work golang:1.24 go test ./internal/slack ./internal/cmd`
- Full verification:
  - `docker run --rm -v "$PWD:/work" -w /work golang:1.24 go test ./...`
- CLI build:
  - `docker run --rm -v "$PWD:/work" -w /work golang:1.24 sh -c 'git config --global --add safe.directory /work && go build -ldflags "-s -w" -o ./lark ./cmd/lark'`

### Files Modified
- `internal/slack/client.go` - added Slack message output types plus send, history, and thread APIs.
- `internal/slack/client_test.go` - added Slack Web API tests for history and thread requests.
- `internal/cmd/slack.go` - added `slack msg send`, `slack msg history`, and `slack msg thread`.
- `internal/cmd/slack_test.go` - added command registration coverage for Slack message commands.
- `slack-migration.md` - updated Phase 3 status and implementation notes.
- `devlogs/2026-06-09.md` - appended this session entry.
